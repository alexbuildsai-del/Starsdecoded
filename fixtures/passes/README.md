# Pass fixtures

A stored blind report and the amendment each of its sections received, replayed by `api/src/lib/horizonPass.test.ts` against the chart computed from the named chart fixture. Birth data lives in `../charts/`; nothing here is fabricated.
